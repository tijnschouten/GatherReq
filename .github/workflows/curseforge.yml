name: CurseForge Release

on:
  push:
    tags:
      - "v*"

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger CurseForge packaging
        run: curl -fsS -X POST "${{ secrets.CURSEFORGE_WEBHOOK_URL }}"
